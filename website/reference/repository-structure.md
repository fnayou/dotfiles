# Repository Structure

This page will document the repository layout: the `stow/` package areas, `docs/`, scripts, and
the Claude Code operating layer.

## TODO

- [ ] Describe the `stow/{common,macos,arch}` layout.
- [ ] Describe `docs/` and the public `website/`.
- [ ] Adapt the structure section from the repository README.
