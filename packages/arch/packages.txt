# Arch / EndeavourOS — package list
# For macOS see: packages/Brewfile
# For Debian (stable / trixie) see: packages/debian/packages.txt
#
# Note: zinit is NOT listed here. Install via one-time manual git clone.
# See: docs/guides/packages-setup.md

# --- Repository prerequisites (pacman) ---
# sudo pacman -S git stow go-task

# --- Changelog tooling (pacman, extra repo) ---
# Only needed to run `task changelog`, which regenerates CHANGELOG.md from
# Conventional Commits. See: docs/decisions/0055-git-cliff-changelog-generation.md
# sudo pacman -S git-cliff

# --- Shell runtime / zsh (pacman) ---
# sudo pacman -S fzf zoxide eza bat

# --- Neovim (pacman) ---
# node (nodejs/npm) + python power the LSP servers Mason installs inside Neovim.
# base-devel provides the C compiler required for treesitter parsers.
# sudo pacman -S neovim ripgrep fd nodejs npm python python-pipx base-devel
# nvim-treesitter (main branch) also needs the tree-sitter CLI:
#   sudo pacman -S tree-sitter-cli      # (or: npm install -g tree-sitter-cli)

# --- System monitor (pacman) ---
# sudo pacman -S btop

# --- Terminal workspace manager ---
# Optional — only needed if you stow the `herdr` package.
# Not in the official Arch repos. Either Homebrew or the AUR package below.
# See: docs/guides/herdr-setup.md
# brew install herdr

# --- AUR packages (requires yay or paru) ---
# yay -S oh-my-posh-bin
# yay -S herdr            # alternative to `brew install herdr`
